name: Build and Release (Windows)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version_tag:
        description: '版本标签 (例如: v1.4.4)'
        required: true
        default: 'v1.0.0'

permissions:
  contents: write

env:
  APP_NAME: SupplyChain-Reconciler-Plus
  PLATFORM: windows-x64

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set tag variable
        id: set_tag
        shell: pwsh
        run: |
          # 安全地设置标签变量（PowerShell 版本）
          if ("${{ github.event_name }}" -eq "push" -and "${{ github.ref }}" -like "refs/tags/*") {
            $tag = "${{ github.ref }}".Replace("refs/tags/", "")
          }
          elseif ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $tag = "${{ github.event.inputs.version_tag }}"
          }
          else {
            $tag = "v1.0.0"
          }

          # 清除换行和非法字符
          $cleanTag = ($tag -replace "[`r`n]", "") -replace "[^a-zA-Z0-9._-]", "-"

          "TAG=$cleanTag" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "tag=$cleanTag" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
          Write-Host "Using tag: $cleanTag"

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 20
          max_attempts: 3
          retry_on: error
          shell: pwsh
          command: |
            python -m pip install --upgrade pip
            pip install -r requirements.txt
            pip install pyinstaller

      - name: Build executable with PyInstaller
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 20
          max_attempts: 2
          retry_on: error
          shell: pwsh
          command: |
            pyinstaller --noconfirm --onefile --windowed --name ${{ env.APP_NAME }} main.py

      - name: Package and generate hashes
        shell: pwsh
        run: |
          $tag = "${{ env.TAG }}"
          $distDir = Join-Path $PWD 'dist'
          $rawExe = Join-Path $distDir "${{ env.APP_NAME }}.exe"

          if (-not (Test-Path $rawExe)) {
            throw "Executable not found: $rawExe"
          }

          $exeName = "${{ env.APP_NAME }}-$tag-${{ env.PLATFORM }}.exe"
          $zipName = "${{ env.APP_NAME }}-$tag-${{ env.PLATFORM }}.zip"
          $hashName = "${{ env.APP_NAME }}-$tag-${{ env.PLATFORM }}.sha256"

          $exeOut = Join-Path $distDir $exeName
          $zipOut = Join-Path $distDir $zipName
          $hashOut = Join-Path $distDir $hashName

          Copy-Item $rawExe $exeOut -Force

          $exeSize = (Get-Item $exeOut).Length
          if ($exeSize -lt 1MB) {
            throw "EXE size too small ($exeSize bytes), build may be invalid"
          }

          Compress-Archive -Path $exeOut -DestinationPath $zipOut -Force

          $exeHash = (Get-FileHash $exeOut -Algorithm SHA256).Hash.ToLower()
          $zipHash = (Get-FileHash $zipOut -Algorithm SHA256).Hash.ToLower()

          @(
            "$exeHash *$exeName",
            "$zipHash *$zipName"
          ) | Set-Content -Path $hashOut -Encoding ascii

          "EXE_FILE=$exeName" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "ZIP_FILE=$zipName" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "HASH_FILE=$hashName" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

          Write-Host "✓ Packaged: $exeOut"
          Write-Host "✓ Packaged: $zipOut"
          Write-Host "✓ Hash file: $hashOut"

      - name: Validate artifact checksums
        shell: pwsh
        run: |
          $hashPath = Join-Path $PWD "dist\${{ env.HASH_FILE }}"
          if (-not (Test-Path $hashPath)) {
            throw "Checksum file not found: $hashPath"
          }

          $lines = Get-Content $hashPath | Where-Object { $_.Trim() -ne '' }
          if ($lines.Count -lt 2) {
            throw "Checksum file content invalid: expected 2 lines"
          }

          foreach ($line in $lines) {
            if ($line -notmatch '^[0-9a-f]{64} \*.+$') {
              throw "Invalid checksum line format: $line"
            }
          }

          Write-Host "✓ Checksum format validated"

      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.TAG }}
          name: Release ${{ env.TAG }}
          draft: false
          prerelease: false
          files: |
            dist/${{ env.EXE_FILE }}
            dist/${{ env.ZIP_FILE }}
            dist/${{ env.HASH_FILE }}
          body: |
            # Supply Chain Reconciler Plus ${{ env.TAG }}
            
            ## 📦 下载
            - **${{ env.ZIP_FILE }}**: 可执行文件压缩包
            - **${{ env.EXE_FILE }}**: 单文件可执行程序
            - **${{ env.HASH_FILE }}**: SHA256 校验文件
            
            ## 🚀 使用说明
            1. 下载并解压
            2. 双击运行 `${{ env.EXE_FILE }}`
            
            ## 🔐 完整性校验
            可使用 `${{ env.HASH_FILE }}` 对下载文件进行 SHA256 校验。
            
            ## ⚠️ 注意事项
            - 仅支持 Windows 系统
            - 首次运行时可能需要管理员权限
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}