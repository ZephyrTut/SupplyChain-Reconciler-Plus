# ⚙️ 配置面板

**QtConfigPanel 详细说明**

---

## 📋 面板概述

配置面板是步骤2的核心组件，包含所有对账规则配置。

### 可折叠区域

面板由多个可折叠的配置区组成，支持展开/折叠操作。

---

## 📑 主键映射区

### 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ▼ 📑 主键映射 (必填)                                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  映射1: 手工表列 [订单编号 ▼]  ↔  系统表列 [订单号 ▼]    [➕] [➖]       │
│  映射2: 手工表列 [物料编码 ▼]  ↔  系统表列 [零件号 ▼]    [➕] [➖]       │
│                                                                         │
│                                    [+ 添加映射]                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 组件说明

| 组件 | 类型 | 说明 |
|------|------|------|
| 手工表列下拉框 | QComboBox | 选择手工表的主键列 |
| 系统表列下拉框 | QComboBox | 选择系统表的对应列 |
| 添加按钮 | QPushButton | 添加新映射行 |
| 删除按钮 | QPushButton | 删除当前映射行 |

### 操作流程

1. 点击手工表列下拉框选择列
2. 点击系统表列下拉框选择对应列
3. 如需多字段主键，点击「+ 添加映射」

---

## 📊 数值列映射区

### 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ▼ 📊 数值列映射 (必填)                                                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  手工表数量列: [数量 ▼]                                                 │
│  系统表数量列: [执行数量 ▼]                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 自动识别

系统会自动识别数值列：
- 包含"数量"、"金额"、"qty"等关键词的列
- 数据类型为数字的列

---

## 🧹 数据清洗区

### 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ▼ 🧹 数据清洗 (可选)                                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 规则1                                               [删除规则]  │   │
│  ├─────────────────────────────────────────────────────────────────┤   │
│  │ 表类型: [手工表 ▼]                                              │   │
│  │ 目标列: [到货单号 ▼]                                            │   │
│  │ 清洗模式: [删除匹配 ▼]                                          │   │
│  │ 正则表达式:                                                     │   │
│  │ ┌───────────────────────────────────────────────────────────┐   │   │
│  │ │ -\d+$                                                     │   │   │
│  │ └───────────────────────────────────────────────────────────┘   │   │
│  │ 快速预设: [末尾序号 ▼]                                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│                              [+ 添加清洗规则]                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 清洗模式

| 模式 | 说明 |
|------|------|
| 删除匹配 | 删除正则匹配到的内容 |
| 保留匹配 | 只保留正则匹配到的内容 |
| 替换为 | 将匹配内容替换为指定值 |

### 快速预设

| 预设名 | 正则表达式 | 说明 |
|--------|-----------|------|
| 末尾序号 | `-\d+$` | 删除末尾的 -数字 |
| 首尾空格 | `^\s+|\s+$` | 删除首尾空格 |
| 非数字 | `\D+` | 删除所有非数字 |

---

## 🔍 筛选条件区

### 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ▼ 🔍 筛选条件 (可选)                                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  手工表筛选:                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ [状态 ▼] [等于 ▼] [已审核        ]                   [➖]       │   │
│  │ [类型 ▼] [包含于 ▼] [正常,补货   ]                   [➖]       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                    [+ 添加筛选]                          │
│                                                                         │
│  系统表筛选:                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ [执行状态 ▼] [不等于 ▼] [已取消      ]               [➖]       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                    [+ 添加筛选]                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 操作符说明

| 操作符 | 值格式 | 说明 |
|--------|--------|------|
| 等于 | 单值 | `状态 = 已审核` |
| 不等于 | 单值 | `状态 ≠ 已取消` |
| 包含 | 单/多值 | 列值包含输入值 |
| 不包含 | 单/多值 | 列值不包含输入值 |
| 包含于 | 多值(逗号分隔) | `已完成,已发货` |
| 大于 | 数值 | `数量 > 0` |
| 小于 | 数值 | `数量 < 1000` |

---

## 📈 透视配置区

### 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ▼ 📈 透视配置 (可选)                                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 📊 手工表透视 (出入库)                             [启用 ☑]       │ │
│  ├───────────────────────────────────────────────────────────────────┤ │
│  │ 透视列: [业务类型 ▼]                                              │ │
│  │ 出库值: [☑ 发货] [☑ 退货] [☐ 退仓] [☐ 入库]                      │ │
│  │ 入库值: [☐ 发货] [☐ 退货] [☑ 退仓] [☑ 入库]                      │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │ 📈 系统表透视                                      [启用 ☑]       │ │
│  ├───────────────────────────────────────────────────────────────────┤ │
│  │ 透视列: [订单状态 ▼]                                              │ │
│  │ 透视值: 已完成, 处理中, 待审核 (自动加载)                         │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 透视类型对比

| 特性 | 手工表透视 | 系统表透视 |
|------|-----------|-----------|
| 用途 | 出入库区分 | 状态展开 |
| 计算 | Σ出库 - Σ入库 | Σ各状态 |
| 配置 | 选出库值+入库值 | 选透视列 |

---

## 🧮 差值公式区

### 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ▼ 🧮 差值公式 (必填)                                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  快速选择: [手工数量 - 系统总计 ▼]                                      │
│                                                                         │
│  自定义公式:                                                            │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 手工数量 - 系统总计                                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  可用变量: 手工数量, 系统总计, 已完成, 处理中, 待审核                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 可用变量

根据透视配置动态更新：

- **无透视**: `手工数量, 系统总计`
- **有透视**: `手工数量, 系统总计, 透视值1, 透视值2, ...`

---

## 📊 数据样例区

### 界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ ▼ 📊 数据样例                                                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  手工表样例 (5行):                      系统表样例 (5行):               │
│  ┌────────────────────────┐            ┌────────────────────────┐      │
│  │ 订单 | 物料 | 数量     │            │ 订单 | 零件 | 状态 | 数量│      │
│  │ A001 | M001 | 100     │            │ A001 | M001 | 已完成| 80│      │
│  │ ...  | ...  | ...     │            │ ...  | ...  | ... | ..│      │
│  └────────────────────────┘            └────────────────────────┘      │
│                                                                         │
│  [导出手工表预处理预览]    [导出系统表预处理预览]                        │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 样例功能

| 功能 | 说明 |
|------|------|
| 数据预览 | 显示前5行数据 |
| 列名参考 | 便于选择配置项 |
| 预处理导出 | 导出清洗/筛选后的数据 |

---

## 🔌 配置API

### 获取配置

```python
config = panel.get_config()

# 返回格式
{
    "key_mappings": [
        {"manual_col": "订单编号", "system_col": "订单号"},
        {"manual_col": "物料编码", "system_col": "零件号"}
    ],
    "value_mapping": {
        "manual_col": "数量",
        "system_col": "执行数量"
    },
    "clean_rules": [
        {
            "table_type": "manual",
            "column": "到货单号",
            "mode": "删除匹配",
            "regexes": ["-\\d+$"]
        }
    ],
    "manual_filters": [
        {
            "column": "状态",
            "operator": "等于",
            "value": "已审核"
        }
    ],
    "system_filters": [...],
    "manual_pivot_config": {
        "enabled": true,
        "pivot_column": "业务类型",
        "out_values": ["发货", "退货"],
        "in_values": ["退仓"]
    },
    "system_pivot_config": {
        "enabled": true,
        "pivot_column": "订单状态"
    },
    "difference_formula": "手工数量 - 系统总计"
}
```

### 设置配置

```python
panel.set_config(config)
```

### 刷新列选项

```python
# 文件导入后刷新
panel.refresh_columns(
    manual_columns=["订单编号", "物料编码", "数量"],
    system_columns=["订单号", "零件号", "执行数量", "订单状态"]
)
```

---

## 💻 技术实现

### 核心类结构

```python
class QtConfigPanel(QWidget):
    # 信号
    config_changed = pyqtSignal()  # 配置变化信号
    
    def __init__(self, parent=None):
        super().__init__(parent)
        self._init_ui()
    
    def _init_ui(self):
        # 创建各区域
        self._create_key_mapping_section()
        self._create_value_mapping_section()
        self._create_clean_section()
        self._create_filter_section()
        self._create_pivot_section()
        self._create_formula_section()
        self._create_sample_section()
    
    def get_config(self) -> dict:
        """获取当前配置"""
        ...
    
    def set_config(self, config: dict):
        """设置配置"""
        ...
    
    def refresh_columns(self, manual_cols, system_cols):
        """刷新列选项"""
        ...
```

### 折叠区域实现

```python
class CollapsibleSection(QWidget):
    def __init__(self, title, parent=None):
        super().__init__(parent)
        self.toggle_button = QPushButton(f"▼ {title}")
        self.content = QWidget()
        self.toggle_button.clicked.connect(self._toggle)
    
    def _toggle(self):
        self.content.setVisible(not self.content.isVisible())
        icon = "▼" if self.content.isVisible() else "▶"
        self.toggle_button.setText(f"{icon} {self.title}")
```

### NoScrollComboBox

禁用滚轮的下拉框，避免影响外部滚动：

```python
class NoScrollComboBox(QComboBox):
    def wheelEvent(self, event):
        event.ignore()  # 忽略滚轮事件
```

---

## ▶️ 下一步

了解结果显示，查看 [结果预览](./12-结果预览.md)。
