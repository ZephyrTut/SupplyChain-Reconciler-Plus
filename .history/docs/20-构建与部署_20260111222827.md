# 🚀 构建与部署

**项目构建、打包与部署指南**

---

## 📋 目录

- [开发环境搭建](#开发环境搭建)
- [源码运行](#源码运行)
- [打包与发布](#打包与发布)
- [生产部署](#生产部署)
- [故障排查](#故障排查)

---

## 🔧 开发环境搭建

### 系统要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10 / macOS 10.14 / Ubuntu 20.04 | 最新版本 |
| Python | 3.10 | 3.11 或 3.12 |
| 内存 | 2GB | 4GB+ |
| 磁盘 | 500MB | 1GB+ |

### 1. 安装Python

#### Windows
```bash
# 从 https://www.python.org/downloads/ 下载
# 或使用 conda
conda create -n reconciler python=3.11
conda activate reconciler
```

#### macOS
```bash
# 使用 Homebrew
brew install python@3.11

# 或使用 conda
conda create -n reconciler python=3.11
conda activate reconciler
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt-get install python3.11 python3.11-venv python3.11-dev

# Fedora
sudo dnf install python3.11 python3.11-devel
```

### 2. 克隆项目

```bash
# 使用 Git
git clone https://github.com/your-org/SupplyChain-Reconciler-Plus.git
cd SupplyChain-Reconciler-Plus

# 或者下载 ZIP
# 然后解压到项目目录
```

### 3. 创建虚拟环境

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# macOS/Linux
python3 -m venv venv
source venv/bin/activate
```

### 4. 安装依赖

```bash
# 升级 pip
pip install --upgrade pip

# 安装项目依赖
pip install -r requirements.txt

# 安装开发依赖（可选）
pip install pytest pytest-cov
```

### 5. 验证安装

```bash
# 运行测试验证环境
python -m pytest tests/ -v

# 或直接启动应用
python main.py
```

---

## ▶️ 源码运行

### 方式1: 直接运行 main.py

```bash
# 确保虚拟环境已激活
python main.py
```

**期望输出**:
- 主窗口打开
- 应用标题: "SupplyChain-Reconciler-Plus v1.4.3"

### 方式2: 使用启动菜单

```bash
# 运行启动脚本
python start.py

# 选择选项：
# 1. 启动应用
# 2. 运行测试
# 3. 查看测试数据
# 4. 显示文档
# 5. 检查环境
```

### 方式3: 使用 shell 脚本

```bash
# Windows
run_tests.bat

# macOS/Linux
chmod +x run_tests.sh
./run_tests.sh
```

---

## 📦 打包与发布

### 方式1: PyInstaller (生成独立可执行文件)

#### 安装 PyInstaller

```bash
pip install pyinstaller
```

#### 构建可执行文件

```bash
# Windows - 生成 exe
pyinstaller --onefile --windowed \
  --name "SupplyChain-Reconciler-Plus" \
  --icon=assets/icon.ico \
  main.py

# macOS - 生成 app
pyinstaller --onefile --windowed \
  --name "SupplyChain-Reconciler-Plus" \
  --icon=assets/icon.icns \
  main.py

# Linux - 生成可执行文件
pyinstaller --onefile \
  --name "SupplyChain-Reconciler-Plus" \
  main.py
```

**输出位置**: `dist/` 目录

#### 生成的文件

| 平台 | 输出文件 | 大小 |
|------|---------|------|
| Windows | SupplyChain-Reconciler-Plus.exe | ~150MB |
| macOS | SupplyChain-Reconciler-Plus.app | ~200MB |
| Linux | SupplyChain-Reconciler-Plus | ~150MB |

### 方式2: 创建安装程序

#### Windows - 使用 NSIS

```bash
# 安装 NSIS (https://nsis.sourceforge.io/)
# 创建 installer.nsi 脚本

# 编译安装程序
makensis installer.nsi

# 输出: SupplyChain-Reconciler-Plus-Setup.exe
```

#### macOS - 创建 DMG

```bash
# 使用 dmgbuild
pip install dmgbuild

dmgbuild -s settings.py "SupplyChain-Reconciler-Plus" output.dmg
```

### 方式3: Docker 容器化（可选）

```dockerfile
# 创建 Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

```bash
# 构建镜像
docker build -t reconciler:1.4.3 .

# 运行容器
docker run -it reconciler:1.4.3
```

---

## 🌍 生产部署

### 部署场景1: 单机部署

```bash
# 1. 解压应用
unzip SupplyChain-Reconciler-Plus-v1.4.3.zip
cd SupplyChain-Reconciler-Plus

# 2. 安装依赖
pip install -r requirements.txt

# 3. 创建配置目录
mkdir -p ~/.config/SupplyChain-Reconciler-Plus

# 4. 启动应用
python main.py
```

### 部署场景2: 批量部署（企业）

#### 使用 Group Policy (Windows)

```batch
@echo off
REM 企业部署脚本

set INSTALL_DIR=C:\Program Files\SupplyChain-Reconciler-Plus
set SOURCE_DIR=\\network\share\reconciler

REM 复制文件
xcopy "%SOURCE_DIR%\*" "%INSTALL_DIR%\" /E /I /Y

REM 创建快捷方式
powershell -Command "
$WshShell = New-Object -ComObject WScript.Shell
$Shortcut = $WshShell.CreateShortcut('$env:APPDATA\Microsoft\Windows\Start Menu\Programs\SupplyChain-Reconciler.lnk')
$Shortcut.TargetPath = '%INSTALL_DIR%\SupplyChain-Reconciler-Plus.exe'
$Shortcut.Save()
"

echo 部署完成
```

#### 使用 Ansible (Linux)

```yaml
---
- name: Deploy SupplyChain-Reconciler-Plus
  hosts: all
  vars:
    app_version: "1.4.3"
    install_dir: "/opt/reconciler"
  
  tasks:
    - name: Create install directory
      file:
        path: "{{ install_dir }}"
        state: directory
        mode: '0755'
    
    - name: Download and extract
      unarchive:
        src: "https://github.com/your-org/releases/v{{ app_version }}/app.tar.gz"
        dest: "{{ install_dir }}"
        remote_src: yes
    
    - name: Install dependencies
      pip:
        requirements: "{{ install_dir }}/requirements.txt"
        virtualenv: "{{ install_dir }}/venv"
    
    - name: Create systemd service
      template:
        src: reconciler.service.j2
        dest: /etc/systemd/system/reconciler.service
      notify: start reconciler
    
    - name: Enable service
      systemd:
        name: reconciler
        enabled: yes
        daemon_reload: yes
```

### 配置文件管理

#### 配置文件位置

| 平台 | 位置 |
|------|------|
| Windows | `%APPDATA%\SupplyChain-Reconciler-Plus\` |
| macOS | `~/Library/Application Support/SupplyChain-Reconciler-Plus/` |
| Linux | `~/.config/SupplyChain-Reconciler-Plus/` |

#### 关键文件

```
~/.config/SupplyChain-Reconciler-Plus/
├── templates.json          # 用户模板
├── settings.ini           # 应用设置（可选）
└── cache/                 # 临时缓存
    └── recent_files.json  # 最近打开文件
```

### 备份与恢复

```bash
# 备份用户配置
cp -r ~/.config/SupplyChain-Reconciler-Plus ~/backup/reconciler_backup

# 恢复配置
cp -r ~/backup/reconciler_backup ~/.config/SupplyChain-Reconciler-Plus
```

---

## 🔒 安全考虑

### 1. 数据安全

```
✅ 不存储敏感信息（密码、密钥）
✅ 用户数据仅本地存储
✅ 不上传数据到网络
✅ 支持数据备份
```

### 2. 文件权限

```bash
# Linux - 限制文件权限
chmod 750 ~/.config/SupplyChain-Reconciler-Plus
chmod 640 ~/.config/SupplyChain-Reconciler-Plus/templates.json
```

### 3. 依赖安全

```bash
# 检查依赖漏洞
pip install safety
safety check

# 定期更新依赖
pip install --upgrade -r requirements.txt
```

---

## 📊 监控与维护

### 应用日志

```python
# 启用日志记录（可选扩展）
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('reconciler.log'),
        logging.StreamHandler()
    ]
)
```

### 性能监控

```bash
# 监控内存使用
top -p $(pgrep -f main.py)

# Windows 任务管理器
tasklist /v | findstr python.exe
```

### 定期维护

| 任务 | 频率 |
|------|------|
| 更新依赖 | 每月 |
| 备份配置 | 每周 |
| 清理缓存 | 每月 |
| 检查日志 | 每周 |

---

## 🆘 故障排查

### 常见问题

#### 1. Python 找不到

```bash
# Windows
python -m pip --version

# 确保 Python 在 PATH 中
echo %PATH%
```

#### 2. 依赖安装失败

```bash
# 升级 pip
pip install --upgrade pip

# 清除缓存
pip install --no-cache-dir -r requirements.txt
```

#### 3. 应用启动失败

```bash
# 检查 Python 版本
python --version

# 运行诊断
python -c "import config.settings; print('OK')"
```

#### 4. PyQt6 相关错误

```bash
# 重新安装 PyQt6
pip uninstall PyQt6 -y
pip install PyQt6>=6.0.0
```

### 诊断工具

```bash
# 运行环境检查脚本
python start.py
# 选择选项 5: 检查环境

# 运行测试
python -m pytest tests/ -v
```

### 获取帮助

1. 查看 [常见问题](19-常见问题.md)
2. 检查日志文件
3. 报告 Issue: https://github.com/your-org/issues

---

## 📝 升级指南

### 从旧版本升级

```bash
# 1. 备份当前配置
cp -r ~/.config/SupplyChain-Reconciler-Plus ~/backup/

# 2. 下载新版本
git pull origin main  # 或下载最新 release

# 3. 更新依赖
pip install -r requirements.txt --upgrade

# 4. 测试
python -m pytest tests/

# 5. 启动应用
python main.py
```

### 版本兼容性

| 从版本 | 升级到 | 破坏性变化 |
|--------|--------|----------|
| v1.4.1 | v1.4.2 | 否（Bug修复） |
| v1.4.2 | v1.4.3 | 否（文档更新） |
| v1.3.x | v1.4.x | 是（UI框架变更） |

---

## 📚 相关文档

- [快速开始](01-快速开始.md)
- [安装配置](02-安装配置.md)
- [常见问题](19-常见问题.md)

---

**最后更新**: 2026年1月11日  
**文档版本**: 1.4.3
